# Runtime dependencies of the server variant. The single-file application in
# mmo_vault/public_html/ has none and never will.
fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
alembic>=1.14
argon2-cffi>=23.1
webauthn>=3.0
authlib>=1.3
itsdangerous>=2.2   # required by the OAuth session middleware
jinja2>=3.1         # the pages of the service; the vault application uses none
